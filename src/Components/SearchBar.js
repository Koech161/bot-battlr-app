import React from 'react'

const SearchBar = ({bots}) => {

  return (
    <div>
      <input 
      type='search'
      placeholder='search bot by name...'
    />
    </div>
  )
}

export default SearchBar
